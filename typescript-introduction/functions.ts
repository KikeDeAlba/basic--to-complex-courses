
// Sentencia "Function" -> nombre -> propiedad -> bloque de codigo
function functionName(prop1: number) {}

// Sentencia "const" -> nombre -> propiedad -> arrow -> bloque de codigo
const arrayFunction = (props1: number) => {
    return props1 + 1
}

// Sentencia "const" -> nombre -> propiedad -> arrow -> expresion
const expressionFunction = (props1: number) => props1 + 1;